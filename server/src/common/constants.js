const Constants = Object.freeze({
    YARD: {
        TYPE: [5, 7, 11],
    },
    STATUS: {
        PENDING: 'pending',
        PAID: 'paid',
        CANCELLED: 'cancelled',
    },
});

module.exports = Constants;
